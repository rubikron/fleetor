---
id: D-087
title: "Claude Code first, codex after: the suite is finished on one harness before the second is made to match"
phase: "meta"
status: alive
---

- **What.** Operator's call, 2026-09-11: build the full History/reopen story end to end on Claude Code, then solve the same problems for codex. Standing instruction for this arc, not a one-off — WP-27's S4 (codex parity, the `tui.resume_cwd` re-check, the C39 allowlist question) and WP-28's codex equivalents wait behind it.
- **What it does *not* license.** The `Harness` seam stays harness-shaped: checkpoint methods are added for every registered harness (codex's `has_session` shipped with R21), and the conformance suite keeps running over both. This orders the *product* work, not the seam.
- **Live consequence, accepted:** `tests/vendor_binary_tier.rs`'s codex arms are red and stay red for now — `typing-tuned-submits` twice, and `brief-replaces` once on 2026-09-11. The probe is stdlib-only and reads no repo code, so neither is caused by the shell changes landing in the meantime. Tracked in WP-27's open question 9.
