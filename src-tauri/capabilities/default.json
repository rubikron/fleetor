{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core events, the pane commands, and the folder picker the target chooser needs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
